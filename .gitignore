# GitHub rejects files > 100 MB; keep large archives out of the repo
hanwen_grasping/archive/Starter_code.zip

# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment variables & secrets (do not commit)
.env
.env.*
!.env.example

# Jupyter
.ipynb_checkpoints/

# Logs & temp
*.log
*.tmp
*.swp
*~

# OS / editor
.DS_Store
Thumbs.db
.idea/

# ML / experiment tooling (local runs)
wandb/
mlruns/
lightning_logs/

# Training checkpoints & run outputs (large; regenerate locally)
ntrl-demo/Experiments/

# Collected robot trajectories (HDF5); large; keep local
collected_data/

# Runtime outputs (eval videos, logs, local experiment artifacts)
output/

# Collected runtime outputs from grasping scripts
hanwen_grasping/saved_as_result/

# Large meshes / URDF / point clouds — keep locally or fetch via script/release
hanwen_grasping/assets/

# Scene & robot datasets — download or prepare locally; paths stay in code
ntrl-demo/datasets/

*.mp4

*.pt

hanwen_grasping/checkpoints/

*.pth
!models/

# Segment Anything ViT-H weights (~2.4 GB); download per img2objloc_model/README.md
img2objloc_model/checkpoints/sam_vit_h_4b8939.pth

NTField_scaling/merged_h5/

archive/